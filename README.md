# fast_bitonic_sort

## Windows
```
git clone https://github.com/vassvik/fast_bitonic_sort.git
cd fast_bitonic_sort
git submodule update --init --recursive
cd Odin
build.bat 1
cd ..
build.bat
```

## Linux

Install glfw from your favourite package manager, and it should hopefully link properly.
